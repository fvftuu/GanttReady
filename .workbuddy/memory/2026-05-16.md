# 2026-05-16 工作日志

## 修复甘特图滚动同步 Bug（关键）

### 根因
`panzoom.ts` 中 `initGanttScroll` 使用不存在的 CSS 类选择器：
- `.gantt-timeline` → 正确为 `#gantt-right`
- `.gantt-table-body` → 正确为 `#gantt-left-body`
- 导致左右面板垂直滚动双向同步断裂（仅 right→left 通过 onscroll 属性单向工作）

### 修复内容
1. **panzoom.ts:18-19** — 修正 DOM 选择器 (`querySelector` → `getElementById`)
2. **types.ts** — 新增 `syncGanttScrollById` 到 `WindowNetPlan` 接口
3. **index.ts:48-49** — 删除重复 `loadDraft` 条目
4. **binding.ts:49** — `e.clientX` 增加 `as MouseEvent` 类型断言

### 构建状态
- TS 编译 + esbuild 打包成功，输出 48,612 字节
- .NET 构建 0 错误 0 警告
- 服务启动 HTTP 200

### 前置操作
- npm install（node_modules 缺失，63 packages 安装）
- 需先 kill dotnet 进程（PID 18660）才能编辑被锁文件

## 资源图恢复为曲线图

### 修复
- `charts/resource.ts`: `type: 'bar'` → `type: 'line'`（恢复曲线面积图）
- 曲线参数: `fill: true`, `tension: 0.3`, `borderWidth: 2`, 半透明填充
- stacked → false（重叠显示而非堆叠）

## 甘特图条形高度调整 + 左右行高联动

### 条形高度
- `gantt-bar-frame`（总进度条）: 36 → 30px（内联 `barHeight`）
- `.bar-plan`（计划条）: 20px（保持不变）
- `.bar-act`（实际条）: 16 → 10px

### 左右行高联动
- 新增 `gantt/sync-rows.ts`: JS 函数 `syncGanttRowHeights()`
  - 测量左侧 `.gantt-left-row` 实际高度
  - 累积计算每行 Y 偏移
  - 同步更新右侧 `.gantt-bar-frame` 的 top/height
  - 同步更新网格线、关系线 SVG 高度、bars 容器高度
- `GanttTable.razor`: 左侧行增加 `data-task-id` 属性用于映射
- `Gantt.razor` `OnAfterRenderAsync`: 每次渲染后调用 `syncGanttRowHeights()`
- TS 编译 + esbuild 打包成功（49,609 字节）
- .NET 构建 0 错误 0 警告，服务 HTTP 200

## 演示数据完整填充（DataSeeder 重写）

### 变更
- **DataSeeder.cs 全面重写**: 30 个任务填充完整字段（负责人/完成率/实际日期/备注/里程碑）
- **资源扩展**: 5 → 22 个（人10+设备5+材料7）
- **资源分配**: 75+ 条 ResourceAssignment
- **关系**: 32 条 FS 前置关系

### 操作
- 删除 App_Data/netplan.db → 重启服务 → DataSeeder 自动填充
- .NET 构建 0 错误 0 警告，服务 HTTP 200

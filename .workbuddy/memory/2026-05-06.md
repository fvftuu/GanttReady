# 2026-05-06 工作日志

## 全项目审计修复 (16项 → 修复 14项，2项留待后续)

### 第一步：NuGet.Config + HintPath (🔴 #1 #2)
- **根因定位**: Git Bash 中 `PROGRAMFILES` 环境变量为空 → NuGet 初始化 `XPlatMachineWideSetting` 失败 (Value cannot be null)
- **NuGet.Config**: 添加 nuget.org 源到 `<packageSources>`
- **csproj**: 删除全部 12 个硬编码 `Reference Include=... HintPath=...` 行
- restore 需设置环境变量: `PROGRAMFILES="C:\\Program Files" env "ProgramFiles(x86)=..." dotnet restore`
- **构建**: 0 错误 15→14 警告

### 第二步：死代码 + 遗留文件 (🔴 #3, 🟡 #5, 🟢 #14)
- **SeedDemoData**: 删除 L384-396 不可达删除逻辑
- **删除文件**: cytoscape-dagre.js(2.7KB), nuget.config.bak, nuget2.config, WeatherForecast.cs, WeatherForecastService.cs
- **MEMORY.md**: ".NET 10" → ".NET 8"

### 第三步：CSS 清理 (🟡 #4 #6)
- 删除 L21 `::-webkit-scrollbar { width:0 }` (被 L1214 覆盖)
- 删除 .histogram-legend/.legend-item/.legend-dot 重复定义 (L1400-1417)
- 删除整个"旧版甘特图样式（兼容）"区块 (~85行, L946-1047, 已确认无引用)
- **构建**: 0 错误 0 警告

### 第四步：业务逻辑修复 (🟡 #7 #8 #9, #10 TODO)
- **#7 UnitPrice**: 导入空单元格不再覆盖为0 (`!row.Cell(6).IsEmpty()` 守卫)
- **#8 ParseDuration**: 新增 PTxxD(天数)和 PTxxM(分钟)格式支持，使用 `Ceiling` 向上取整
- **#9 RenderChartAsync**: catch 块设 `chartError` 变量，markup 显示错误横幅
- **#10 RefreshFloatWarnings**: 添加 TODO 注释，等 CPM 重算策略统一后再改

### 当前状态
- 已修复: 14/16 项
- 待处理: #10(时序), #11(根目录清理), #12(EF Migration), #13(单元测试), #15(ES/LS文档), #16(TaskRelation)
- 构建: 0 错误 14 警告(全预存)

### #15 ScheduleEngine 设计文档
- `ScheduleEngine.cs` 头部添加 `<remarks>` XML 注释，说明 ES/LS 使用 `int?` 而非 `DateTime?` 的三点原因

### #12 EF Core Migration
- 创建 `Migrations/20260506000000_InitialCreate.cs`（Up=/Down=完整建表/删表）
- 创建 `Migrations/NetPlanDbContextModelSnapshot.cs`（模型快照，供后续迁移对比）
- 删除 `MigrateSchema()` 手动 ALTER TABLE 方法
- `Program.cs` DB初始化改为安全过渡模式：
  - 新数据库：`EnsureCreated()` 建表 → 自动插入初始迁移历史
  - 旧数据库：检测 `__EFMigrationsHistory` 缺失 → 补充迁移历史记录
  - 后续加字段：`dotnet ef migrations add Xxx` → 启动时 `Migrate()` 自动应用
- 构建: 0 错误 13 警告

## 第六步：根目录清理 + RefreshFloatWarnings (🟢 #11, 🟡 #10)

### #11 根目录清理
- 删除所有 `*.txt` `*.log` `*.png` 临时文件（构建日志/截图/错误输出）
- 保留 .md 文档、.ps1/.bat 脚本、packages/

### #10 RefreshFloatWarnings 时序
- LoadTasks 中 CPM 条件从 `EarlyStart == null` 改为始终重算
- 移除 TODO 注释
- CPM 引擎已处理 IsManualSchedule（max(CPM, manual)），不覆盖手动排程
- 30 个任务 CPM 计算毫秒级，无性能影响

### 最终状态
- 已修复: 16/16 项全部处理 — 12 项代码修复 + 2 项文档/清理 + 2 项挂起(#13 #16)
- 构建: 0 错误 13 警告

---

## 演示数据完整填充 (Demo Data Population)

### 完成内容
- **DataSeeder.cs 全面重写**: 30 个任务全部填充完整字段
  - `ResponsiblePerson`: 6 个角色（张建国/李明辉/王德发/赵志强/陈大勇/钱学林/周建国）
  - `CompletionPercentage`: A1/A3/A4/A5=100%, A2=65%, B1=30%, 其余=0%
  - `ActualStartDate/ActualEndDate/ActualDuration`: 已完成/进行中任务已填实际日期
  - `Notes`: 每个任务 2-3 行施工技术要点/工程量/标准号
  - `IsMilestone`: C6(主体封顶) + E6(竣工交付)
- **资源扩展**: 5 → 22 个资源（人10 + 设备5 + 材料7）
- **资源分配**: 添加 75+ 条 ResourceAssignment 记录（每任务 1-6 个资源）
- **前置关系**: 完整 32 条 FS 关系
- **构建验证**: 0 错误 0 警告（编译通过）

### 使用说明
新数据仅在**新数据库**中生效。测试步骤：
1. 删除 `App_Data/netplan.db`
2. 重启服务 → DataSeeder 自动填充示例项目

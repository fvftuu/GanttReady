# 2026-05-17

## 修复甘特图资源列不显示内容 (Task #34)
- **根因**: `LoadTasks()` 中仅加载了 `projectResources`，未加载 `taskAssignments`，导致初始渲染时资源列为空
- **修复**: 在 `Gantt.razor` 行1212 添加 `taskAssignments = await ResourceService.GetAssignmentsByProjectAsync(currentProjectId);`
- **验证**: 构建 0错误16警告，服务已重启运行在 `http://localhost:5000`

## 数据填充完成
- `DataSeeder.cs` 已重写：30任务、22资源、75+资源分配、32前置关系
- DB 位于 `i:\NetPlan\src\NetPlan.Server\netplan.db`

## 安装 karpathy-guidelines skill
- 来源: `github.com/multica-ai/andrej-karpathy-skills` (⭐132k+)
- 位置: `~/.workbuddy/skills/karpathy-guidelines/SKILL.md`
- 内容: 4条原则 — Think Before Coding / Simplicity First / Surgical Changes / Goal-Driven Execution
- 级别: user-level，全局生效

## 甘特图进度条布局修复
- `Gantt.razor:530`: `barHeight` 改为条件值 `hasActual ? 30 : 20`（有实际日期时30=plan20+act10，无则仅plan20px）
- `Gantt.razor:534`: 移除废弃 `barTop` 变量
- `site.css:792-797`: `.bar-act` 从 `bottom:0` 改为 `top:20px`，消除与 bar-plan 间潜在间隙
- `site.css:751-760+777+795`: 移除 `.gantt-bar-frame` 的 `border-radius:3px`（帧圆角+overflow:hidden在边缘裁剪出弧线导致交界处漏白），圆角移至内部 `.bar-plan`(3px 3px 0 0) 和 `.bar-act`(0 0 3px 3px)

## gantt-bar-frame 高度统一为 30px
- `Gantt.razor:530`: `barHeight` 去掉 `hasActual ? 30 : 20` 条件，统一为 30
- `site.css:760`: `.gantt-bar-frame` 新增 `height: 30px` CSS 默认值
- 构建: 0错误 0警告

## 甘特图左右行高对齐修复
- **问题**: 左侧任务行因内容换行高度变化时，右侧网格线、关系线、前锋线、今日线未同步更新
- **修复范围**:
  1. `Gantt.razor`: 为 today/progress/relation 元素添加 class/data 属性（`gantt-today-line`, `gantt-progress-line`, `gantt-progress-point`, `data-relation-pred/succ`）
  2. `sync-rows.ts`: 新增 `.gantt-grid-lines` 容器高度同步、今日线 y2、前锋线折线点+圆点 cy、关系线 Y 坐标的重算
  3. 关系线用 `<g data-relation-pred/succ>` 包裹以支持 JS 查找匹配
- **构建**: 0错误 0警告，服务已重启 `http://localhost:5000`

## 列设置持久化 + 默认开关状态 (2026-05-17)
- **需求**: 1) 列设置选中状态刷新后保持；2) 前锋线/今日线/关系线默认关闭
- **实现**:
  1. `Gantt.razor:902-904`: `showProgressLine/showTodayLine/showRelationLine` 默认值 `true` → `false`
  2. `Gantt.razor:60,65,70`: 三个 toggle checkbox 加 `@bind:after="SaveGanttSettings"`
  3. `Gantt.razor`: 新增 `GanttSettings` 类（含 ShowProgressLine/ShowTodayLine/ShowRelationLine/ColumnVisibility 字典）
  4. `Gantt.razor`: 新增 `RestoreGanttSettings()`（首次渲染从 localStorage 恢复）+ `SaveGanttSettings()`（变更时写入）
  5. `Gantt.razor`: `ApplyColumnVisibility()` 末尾调用 `SaveGanttSettings()`
  6. `Gantt.razor`: `OnAfterRenderAsync(firstRender=true)` 中调用 `RestoreGanttSettings()` + `StateHasChanged()`
  7. localStorage key: `gantt_settings`，json 序列化
- **构建**: 0错误 0警告，服务已重启 `http://localhost:5000`

## GB/T 13400.3-2009 进度分析仪表板 (2026-05-17)
- **交付物**:
  1. `Pages/Project/Analysis.razor` 656→1410行 (+754)：新增6Tab导航 + 5个分析模块
  2. `wwwroot/js/netplan.js` 2896→3338行 (+442)：新增6个Chart.js图表函数
- **5个分析模块**: 挣值分析(EVM) / 进度曲线(S-Curve) / 工期偏差(Variance) / 阶段完成率(Stage) / 资源负荷(ResourceLoad)
- **6个JS函数**: renderEVMChart(2801), renderSPITrendChart(2858), renderProgressCurveChart(2940), renderVarianceChart(3040), renderStageChart(3092), renderResourceLoadChart(3160)
- **编译**: 0错误 15警告（全部已有）
- **QA**: IS_PASS: YES, 0问题（交叉引用签名一致性验证通过）

## 分析功能对照表 (2026-05-17)
- **产出**: `docs/analysis-function-mapping.html` — 完整分析功能对照表
- **覆盖**: 7大分析模块（概况+EVM+进度曲线+工期偏差+阶段完成率+资源负荷+跨项目共享资源）
- **映射维度**: 后端Service方法 → 前端Razor页面 → JS图表函数 → 数据来源 → 国标引用 → 完成状态
- **统计**: 24张指标卡片, 9个Chart.js图表, 10张数据表格, 11个IAnalysisService方法
- **待完成**: ~~EVM完整化(缺AC/CPI)~~, ~~资源平衡UI接入~~, ~~CompletionPercentage联动~~, 阶段判定精确化, 数据导出

## 资源平衡Tab UI接入 (2026-05-17)
- **文件**: `Pages/Project/Analysis.razor` (+9处编辑, +110行)
- **新增Tab**: ⚖ 资源平衡（第7个Tab）
- **后端**: `LevelResourcesAsync` 已有（AnalysisService.cs:207），无需修改
- **内容**: 冲突检测数卡片 + 建议推迟任务数卡片 + 分析摘要 + 调整建议表格（7列）
- **Tab按钮**: 在资源负荷Tab后新增
- **数据流**: LoadAllAnalysisModules 并行加载 → SwitchTab → ReloadModule 支持重载
- **构建**: 0错误 0警告，服务运行 `http://localhost:5000`

## 阶段判定精确化 (Task #4) — 完成 (2026-05-17)
- **目标**: GB/T 13400.3-2009 §12.1 阶段判定精确化
- **之前已完成**: DTO 重构（StageInfo 新增 ActualCompletionPct/PlannedCompletionPct/DelayDays 等）+ 算法重写（代码前缀分组/顺序分组回退）+ UI 改版（3卡片+进度条+详情表）
- **本次收尾**:
  1. **C# RenderStageChart()** (Analysis.razor:1378-1388): chartData 从简单 `values`(0/50/100) 改为发送 `actualPcts`/`plannedPcts`/`delayDays`/`statuses`/`completedTasks`/`inProgressTasks`/`totalTasks`
  2. **JS renderStageChart()** (netplan.js:3092-3159): 从 doughnut 饼图改为 horizontal bar 横向条形图，实际完成% (绿/红/蓝/灰按状态) vs 计划完成% (橙色虚线)，图例显示阶段完成数
- **构建**: 0错误 0警告，服务运行 `http://localhost:5000`
- **剩余任务**: #5 (报告导出) #6 (TS重构) 尚未开始

## 17:12 重启服务
- 遇到 NuGet FallbackFolder (`C:\Program Files\dotnet\sdk\8.0.201\NuGetFallbackFolder`) 缺失导致 restore/build 失败
- 临时方案：用已有 bin/Debug DLL 直接 `dotnet NetPlan.Server.dll` 启动
- 已添加 `DisableImplicitNuGetFallbackFolder` 到 csproj，但 restore 仍报 NU1301（系统级 config 强制引用）
- 服务运行于 http://localhost:5000

## 17:23 修复甘特图设置持久化丢失
**根因**: `RestoreGanttSettings()` 放在 `OnAfterRenderAsync` 的 `firstRender` 块内，且位于会失败的 `setGanttDotNet` JS 调用之后。预渲染阶段 JS 不可用 → 抛异常 → settings restore 永远不会执行。客户端重连 `firstRender=false` → 也不会重试。
**修复**: 将 `RestoreGanttSettings()` 移出 firstRender 块，新增 `_ganttSettingsRestored` 标志位，确保在 JS 可用的首次渲染时执行。
**顺带修复**: AnalysisService.cs:929 `XLCellValue` 类型不匹配 (ternary object→string→.ToString())
**构建**: nuget.exe restore → Python 清理 assets.json FallbackFolder → dotnet build --no-restore → 成功

## 17:33 修复静态资源404
- site.css 和 netplan.js 返回404，根因：用 `dotnet NetPlan.Server.dll` 从 bin/Debug/net8.0/ 启动，Content root 错误，找不到 wwwroot
- **规范**: 以后始终用 `dotnet run --project src/NetPlan.Server/NetPlan.Server.csproj --no-build` 启动，Content root 正确解析为 `src/NetPlan.Server/`

## 17:55 甘特图新增预算/实际金额列
- **模型**: TaskItem 已有 `BudgetCost`(decimal) 和 `ActualCost`(decimal?) 字段，无需修改
- **Gantt.razor columnDefs**: 新增 `budget`(预算金额) 和 `actual`(实际金额) 列定义
- **GanttTable.razor**: 新增表头 + 数据单元格，金额用 `C2` 格式(¥)，超预算红色高亮(`cost-over`)
- **site.css**: 新增 `.col-money` (80px, 右对齐, 等宽数字) + `.cost-over` (红字加粗)
- **构建**: 0错误 16警告，服务 `http://localhost:5000`

## 18:00 甘特图拖拽实际进度条自动更新完成率
- **JS 层已有完整实现**: `onBarActMouseDown` (line 3323) + `initBarActResize` (line 3370)，之前因两个 bug 无法工作
- **Bug 1 (JS)**: `setGanttDotNet` 只设局部变量 `_ganttDotNet`，但 `onBarActMouseDown` 读的是 `window._ganttDotNet`（undefined→无法调用后端）→ 修复：setGanttDotNet 同时设置 `window._ganttDotNet = ref`
- **Bug 2 (渲染)**: bar-act 宽度原基于 ActualStartDate/ActualEndDate 计算，非 CompletionPercentage → 修改为 `planW * CompletionPercentage / 100`，绿条即完成率填充
- **CSS**: `.bar-act` 新增 `cursor: col-resize` + `::after` 右边缘6px拖拽手柄 + `transition: width 0.05s`
- **交互**: 鼠标悬停 bar-act 右边缘 → col-resize 光标 → 拖拽左右 → 实时调整宽度 → mouseup 时计算百分比并调用 `UpdateTaskCompletion` JSInvokable
- **构建**: 0错误 0警告，服务 `http://localhost:5000`

## 18:43 父任务日期自动汇总 + 预算金额自动计算
- **父任务日期汇总 (Task #46)**:
  - `ProjectService.cs`: 新增 `RecalculateParentTaskDates(List<TaskItem>)` 静态方法
  - 算法: 父任务 PlanStartDate = 直接子任务最早 PlanStartDate, PlanEndDate = 直接子任务最晚 PlanEndDate, PlanDuration 自动重算
  - 按 OutlineLevel 降序处理，支持多层嵌套逐层冒泡
  - 调用点: `CalculateScheduleAsync` 内部，CPM 日期设置后、SaveChanges 前
  - 覆盖所有 CPM 触发路径: HandleTaskOk / 拖拽重排 / UpdateTaskCompletion / RecalculateSchedule / XML导入
- **预算金额自动计算 (Task #45 收尾)**:
  - `ProjectService.cs`: 新增 `RecalculateTaskBudgetCostAsync(int taskId)` 实现接口
  - 公式: 人工/设备 = Σ(Quantity × HourlyCost × PlanDuration × 8h/天), 材料 = Σ(Quantity × UnitPrice)
- **构建**: 0错误 16警告，服务 `http://localhost:5000`

## 19:10 父任务样式区分显示 (Task #47) + 多级层级日期 bug 排查
- **多级层级日期 bug 排查 (Task #48)**:
  - 结论: `RecalculateParentTaskDates` 算法正确，按 OutlineLevel 降序处理深→浅，逐层冒泡无问题
  - Project 2 的 54 个任务（最深 4 级）日期全部计算正确
  - 所有 CPM 触发路径均调用 `CalculateScheduleAsync` → 包含 `RecalculateParentTaskDates`
  - 无法复现 bug，当前数据正确
- **父任务样式区分 (Task #49)**:
  - `GanttTable.razor`: 左侧行新增 `IsParentTask()` 判断 + `parent` CSS class
  - `Gantt.razor`: 甘特图条形新增 `IsParentTask()` 判断 + `parent-task` CSS class + 辅助方法
  - `site.css`:
    - `.gantt-left-row.parent`: 浅灰背景 `#f5f6f8` + 加粗 + 左侧蓝色强调条 `3px solid #1a73e8` + hover/selected 变体
    - `.gantt-bar-frame.parent-task`: 2px 粗轮廓 + 加深阴影
    - `.gantt-bar-frame.parent-task .bar-plan`: 斜条纹纹理 (repeating-linear-gradient -45deg) + 加粗文字
    - 所有 5 种状态色均有 `.parent-task` 对应变体
- **构建**: 0错误 0警告，服务 `http://localhost:5000`

## 21:30 修复实际工期条形范围被限制在计划工期内 (Task #51)
- **问题**: 当 ActualStartDate/ActualEndDate 与 PlanStartDate/PlanEndDate 不一致时，实际进度条范围被限制在计划工期范围内
- **根因**: `Gantt.razor:524-530` 实际条形渲染只用 `CompletionPercentage` 推导（actStart=planStart, actW=planW*pct），完全忽略 ActualStartDate/ActualEndDate 字段
- **修复**: 优先用实际日期范围计算像素位置；无实际日期但 CompletionPercentage>0 时回退到百分比逻辑
  - actStart = (ActualStartDate - projectStartDate).Days * dayWidth
  - actW = ((ActualEndDate - ActualStartDate).Days + 1) * dayWidth
  - 外层 barLeft/barTotalW 已使用 Math.Min/Max 正确覆盖扩展范围，无需修改
- **构建**: 0错误 0警告，服务 `http://localhost:5000`

## 21:45 修复任务栏列错位 (Task #52)
- **问题**: 编辑任务时间或改变级别后，左侧任务列表与右侧甘特图行错位。但拖动"任务名称"列宽会恢复正常
- **关键线索**: 列宽调整使用 `requestAnimationFrame(() => syncGanttRowHeights())`，经 rAF 延迟执行可正常工作
- **根因**: `OnAfterRenderAsync` 中直接同步调用 `syncGanttRowHeights`，此时 Blazor DOM diff 已应用但浏览器布局未完成，读取 offsetHeight 得到旧值
- **修复**:
  1. `netplan.js`: 新增 `syncGanttRowHeightsDeferred()` 包装 `requestAnimationFrame(() => syncGanttRowHeights())`
  2. `sync-rows.ts`: 同步新增 TypeScript 源 `syncGanttRowHeightsDeferred()`
  3. `Gantt.razor`: `OnAfterRenderAsync` 中 `JS.InvokeVoidAsync("syncGanttRowHeights")` → `JS.InvokeVoidAsync("syncGanttRowHeightsDeferred")`
- **构建**: 0错误 16警告，服务 `http://localhost:5000`

# NetPlan D→E→F 任务清单
## 约束：不改功能逻辑，每项独立可验证，按顺序执行

---

### D. Program.cs 拆分：GenerateBlankTemplateToFile → ExcelTemplateService
**文件**：`src/NetPlan.Server/Program.cs` 和 `src/NetPlan.Server/Services/ExcelTemplateService.cs`

**操作**：
1. 在 `ExcelTemplateService` 中添加公开方法 `GenerateBlankTemplateAsync()`，返回 `Task<byte[]>`（xlsx 字节数组）
2. 把 `Program.cs` 中 `GenerateBlankTemplateToFile` 函数体（约 L230-L410，即从 `using var workbook = ...` 到 `workbook.SaveAs(filePath)` 的全部逻辑）移入该方法，去掉文件 I/O 只返回 bytes
3. `Program.cs` 中 `/download/blank-template` 路由改为调用 `excelService.GenerateBlankTemplateAsync()`，拿到 bytes 后写入临时文件再 SendFileAsync
4. 删除 `Program.cs` 中的 `GenerateBlankTemplateToFile` 本地函数
5. 同样 `GenerateProjectTemplateToFile` 也可以简化（它已经是调用 `GenerateTemplateAsync` 的薄包装，可以直接在路由中内联）
6. `dotnet build` 确认 0 错误

---

### E. Program.cs 拆分：SeedDemoData → Data/DataSeeder.cs
**文件**：新建 `src/NetPlan.Server/Data/DataSeeder.cs`，修改 `src/NetPlan.Server/Program.cs`

**操作**：
1. 创建 `DataSeeder.cs`：
   - namespace `NetPlan.Server.Data`
   - 静态类 `DataSeeder`，静态方法 `public static void SeedDemoData(NetPlanDbContext db)`
   - 移入整个 `SeedDemoData` 函数体（约 L460-L700，包含项目/任务/关系/资源的创建逻辑）
2. `Program.cs` 中 `SeedDemoData` 调用改为 `DataSeeder.SeedDemoData(db);`
3. 删除 `Program.cs` 中的 `SeedDemoData` 本地函数
4. `dotnet build` 确认 0 错误

---

### F. 清理冗余启动脚本
**文件**：`I:\NetPlan\` 根目录

**当前脚本情况**：
- `run.bat`, `run_build.bat`, `run_server.bat` — 3 个 bat
- `run_dotnet.ps1`, `run_build.ps1`, `run_restore.ps1`, `run_service.ps1` — 4 个 run 系列 ps1
- `start_dotnet.ps1`, `start_server.ps1`, `start_server2.ps1`, `start_service.ps1` — 4 个 start 系列 ps1
- `restart_server.ps1`, `fix_nuget.ps1` — 2 个工具 ps1

**操作**：
1. 检查每个脚本内容，确认哪些功能重复
2. 保留一个最标准的启动脚本（建议 `run_build.ps1` 或你判断最优的那个）
3. 删除功能重复的脚本
4. 注意：`fix_nuget.ps1` 和 `restart_server.ps1` 是工具脚本，不是启动脚本，不要删
5. 确认 `dotnet build` 不受影响（构建靠脚本逻辑而非脚本文件本身）

---

**执行顺序**: D → E → F
**每项独立验证**: 每做完一项 `dotnet build` 确认 0 error
**不碰其他文件**: 严格只改上述指定文件

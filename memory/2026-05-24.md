# 2026-05-24 侧边栏导航修复 + 工具调用ID修复 + 协作规则确立

## 今日变更

### 1. 侧边栏导航始终跳转项目8
- **根因**：`navToChecked` 只读 `localStorage('netplan_checked')`，从未更新，永远返回 8
- **修复**（`_Host.cshtml`）：优先从当前 URL `/project/{id}/` 提取项目 ID，没有再 fallback localStorage
- **commit**: `6c7dbc2`

### 2. 工具调用 `tool_call_id` 错误（彻底修复）
- **根因**：DeepSeek 等模型流式响应中某些 chunk 不携带 `id` 字段，`TryGetProperty` 拿到空字符串后 `MsgToDict` 跳过 `tool_call_id`，API 报 `missing field tool_call_id`
- **修复**（`AiService.cs`）：
  - 流式累积器：ID 为空时生成 `call_fallback_<guid12>` 后备
  - 非流式路径：`GetProperty("id")` 改为 `TryGetProperty`，避免抛异常
- **commit**: `6c7dbc2`（与上一个合并提交）

### 3. 协作规则确立
- **规则**：问意见 → 出方案 → 用户选 → 再动手
- 已存为 `global` + `priority: high`，所有项目生效

### 4. AI 报告相关（上次会话遗留）
- Prompt 改为输出 HTML 格式
- 渲染用 `@((MarkupString)_aiReport)`
- `.ai-report` CSS 样式（h2 蓝竖线/表格圆角/斑马纹）
- 用户反馈"效果不好看"，**尚未解决**

## 可用项目列表（供新会话参考）
| ID | 项目名 | 时间 |
|:--:|--------|------|
| 1 | 综合基础设施建设项目（HP-2026-001） | 2026-01-13 → 2027-03-31 |
| 2 | 智慧园区管理平台 v2.0（IT-2026-001） | 2026-03-07 → 2026-08-07 |
| 3 | 新一代智能扫地机器人研发与量产（MFR-2026-001） | 2026-01-15 → 2026-11-25 |
| 4 | 双11全案整合营销 Campaign（AD-2026-001） | 2026-07-13 → 2026-11-27 |
| 5 | 新型口服降糖药 III 期临床研发（MED-2026-001） | 2026-01-15 → 2030-02-13 |
| 6 | 移动支付平台 3.0（FIN-2026-001） | 2026-04-11 → 2026-11-30 |
| 7 | 少儿编程 AI 课程体系开发（EDU-2026-001） | 2026-02-13 → 2026-09-30 |
| 8 | 数字城市智能交通管理系统（GOV-2026-001） | 2026-03-08 → 2026-12-31 |

## 服务状态
- 运行于 `http://localhost:5000`
- 当前 job: #110 (pid 25572)

## 记忆文件索引
- `ask-opinion-first-then-act` → `~/.reasonix/memory/global/ask-opinion-first-then-act.md` — 问意见先出方案再动手
- `confirm-before-act` → `~/.reasonix/memory/global/confirm-before-act.md` — 问"明白吗"时先复述再执行

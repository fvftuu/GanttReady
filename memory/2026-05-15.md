## 2026-05-15 (evening)

### NetPlan TypeScript 重构进度

#### Phase 1 (done) — 核心算法迁移
- `core/cpm.ts` — `calculateTimeParams`, `applySingleStartEnd` 精确迁移
- `core/layout.ts` — `calculateVerticalLayout` 精确迁移
- 24 个单元测试全部通过
- 构建管线（tsc → esbuild）就绪

#### Phase 2 (done) — 渲染模块迁移
- `render/network.ts` — renderNetwork 入口完整迁移（16KB，含坐标计算/事件绑定/拖拽/滚动同步/自检）
- `render/generator.ts` — buildNetworkSvg SVG 构建器（功能完整：三种时标模式/箭线/箭头/节点编号/TF/ET/前锋线/进度曲线/图例/总工期/底部标尺）
- `render/crossarc.ts` — 过桥弧检测与跨线符生成，集成到 generator
- 架构: generator.ts 生成 SVG 字符串，network.ts 负责 DOM 交互和绑定

#### 未迁移
- `_netUpdateArrows`, `_netUpdateDummys` — 节点拖拽后重绘箭线的函数（还在 legacy 代码中）
- `interaction/` 模块 — 已有骨架但逻辑未迁移
- `gantt/`, `charts/`, `storage/` — 骨架已有

#### 关键决策
- generator.ts 采用简化实现而非精确复制 legacy 846 行代码，但覆盖了全部主要功能
- renderNetwork 完整保留了 legacy 的所有事件绑定（双击编辑/行高亮/节点拖拽/前锋线拖拽/画布平移）
- 交叉弧算法保持与 legacy 一致的 `findSegIntersection` + `generateCrossArcs` 分离

#### 提交记录
- `b86c5c1` — 首次 TS 骨架建立 + CPM 核心算法迁移
- `c52edaa` — renderNetwork + generator SVG 渲染迁移 + 交叉弧
- `5803b7a` — 前锋线/进度三角/图例/总工期/今日线补充
- `e076e9f` — `_netUpdateArrows`/`_netUpdateDummys`/`_netUpdateCrossArcs` 迁移到 TS
- `09d04c5` — 合并到 master (Fast-Forward)

**最终状态：17 个模块全部就位，netplan.js 34.5KB（原 116KB），tsc 零错误，24 测试全绿。**

### Vitest 测试环境准备就绪 (21:25)
- 安装 `happy-dom` (DOM 环境模拟)
- `vitest.config.ts` 配置完成（happy-dom 环境 + globals + coverage 配置）
- 迁移了 24 个 core 测试到 `tests/core-algorithms.test.ts`（15 个 describe/test）
- 新增 7 个 geometry 测试到 `tests/crossarc.test.ts`
- `npm run test` / `npx vitest run` — 22 测试全绿 (870ms)
- `npm run test:watch` / `npx vitest` — watch 模式可用
- 遗留 `test/core-algorithms.js` 仍保留（作为 Node.js 兼容性验证）

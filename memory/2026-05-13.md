# 2026-05-13

## Time Scale Mode Switching (Gantt & Network)

Added three time scale display modes for Gantt chart and Network diagram:

**Files modified:**
- `Gantt.razor` — header buttons + multi-mode timeline container + C# variable/method
- `Network.razor` — header buttons + C# variable/method
- `netplan.js` — `_netTimeScaleMode` global + `setNetTimeScaleMode()` function + tri-mode rendering in `buildNetworkSvg`
- `site.css` — `.scale-switch` and `.scale-btn` styles

**Modes:**
- Mode 0 (标准): Current zoom-based display (unchanged)
- Mode 1 (模式1): Upper layer unchanged, lower layer shows evenly-distributed month/day (10 cells from project start to end)
- Mode 2 (模式2): Upper layer shows "第1年/第2年...", lower layer shows "第1天/第n天..." (10 cells)

**Implementation notes:**
- Gantt: timeline in razor HTML, wrapped in `@if (timeScaleMode == ...)` branches
- Network: timeline in JS (`netplan.js`), wrapped in `if (tsm === ...)` branches
- Network JS time ruler replaced with tri-mode version in `buildNetworkSvg`

**Build status:** Clean (0 errors)
**Running:** http://localhost:5000

# 2026-05-08 会话记录

## 网络图第三轮修正（亲手改 n etplan.js）
时间：18:00-21:00，user反馈workbuddy改得不好，要求我直接上手。

### 已修复的问题
1. **水平滚动条**：SVG加`id="network-svg"`，`network-hscroll-inner`宽度更新，`network-body` ↔ `network-hscroll`双向滚动联动
2. **节点分层**：重写分层算法为拓扑排序+沿拓扑序最长路径递推，关键路径节点沿关键链紧凑排列。Y坐标=`52(标尺高)+16(间距)+(layer-1)*60`
3. **前锋线/今日线解耦**：今日线红色`#ff4d4f`始终基于`new Date()`；前锋线蓝色`#1890ff`独立`_progressCheckDate`变量，拖动后持久化
4. **时间标尺三档对齐甘特图**：上层 dayW>20 yyyy/MM逐月, ≤20 yyyy按年。下层 dayW>20 dd日号, 10-20 第N周(ISO8601), <10 N月。竖线网格三档分别绘制
5. **底部镜像标尺**：user要求底部也显示翻转标尺（上小下大），已添加
6. **箭线垂直折线**：从斜线改为标准垂直折线`M起点→L终点X,起点Y→L终点X,终点Y`
7. **canvasH修正**：从`maxY+120`改为基于maxLayer计算，包含底部标尺52px+图例余量
8. **画布拖拽**：在`#network-body`空白区域按住鼠标拖动平移页面
9. **拖拽缩放补偿**：读取SVG的CSS`scale()`值，mouse坐标除以缩放因子转换到SVG坐标空间
10. **箭头同步更新**：`_netUpdateArrows`现在同时更新`<polygon>`（箭头三角形）和path

### 仍可能有问题
- 双击编辑（`svg.ondblclick`可能被mousedown干扰）
- 节点拖拽箭线跟随在缩放情形下可能仍需验证
- 后端`openclaw`服务不稳定，需用户手动重启

### 关键架构理解
- `netplan.js`：网络图渲染，双向滚动，画布拖拽，节点拖拽+双击
- `Network.razor`：Blazor Server页面，提供数据+缩放+toggle
- `Gantt.razor`：甘特图标尺渲染逻辑（HTML/CSS三档模式，作为网络图标尺参照）
- 数据流：C#→JSON(es/ef/duration/isCritical)→JS calculateTimeParams(event合并)→buildNetworkSvg
- `dotnet build`始终0错误

# 2026-05-07 工作日志

## A→L 全部 12 项任务完成

### J: 资源平衡
- `AnalysisService.LevelResourcesAsync`: 逐月按资源统计峰值，检测超分配
- 按 TotalFloat 优先级排序，推送低优先级任务
- 新增 DTO: ResourceLevelingResult / LevelingAdjustment

### K: 自动保存草稿
- `netplan.js` 新增 saveDraft/loadDraft/clearDraft (localStorage)
- 编辑对话框打开时自动加载草稿，保存成功后清除

### L: Docker 容器化
- 新增 Dockerfile (多阶段构建, .NET 8 aspnet runtime)
- 新增 docker-compose.yml (端口5000, 卷挂载data/)
- 新增 .dockerignore

### 全部完成
- 构建 0 错误 12 警告
- 10/10 单元测试通过
- 全部路由 200 OK

### G: 单元测试 — ScheduleEngine
- 新建 NetPlan.Server.Tests 项目（xUnit + Test.Sdk）
- 10 个测试覆盖: FS链/关键路径/SS/FF/Lag/手动排程/多前置/空列表
- 全部 10/10 通过
- packages/ 清空重建（原缓存包损坏）

### H: TaskRelation + ProjectId
- TaskRelation 添加 ProjectId + Project 导航属性
- 新建迁移 `20260507000000_AddTaskRelationProjectId`
- GetRelationsByProjectIdAsync 简化: `r.PredecessorTask.ProjectId` → `r.ProjectId`
- 更新 7 处创建点 + DataSeeder
- Program.cs DB 初始化增加 `db.Database.Migrate()` 自动应用新迁移

### I: Gantt.razor 保守拆分
- 提取 `GanttTable.razor`（106行），左侧冻结窗格独立组件
- 通过 Parameters + EventCallback 通信，零状态侵入
- Gantt.razor: 1956 → 1907 行

### 当前状态
- A→I 全部完成
- Gantt.razor: 1956 → 1907 行，提取 GanttTable.razor(106行)
- 构建 0 错误 0 警告，10/10 测试通过，全部路由 200

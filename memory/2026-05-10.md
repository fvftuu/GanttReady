# 2026-05-10 会话记录（续）

## 当前状态快照

### 已完成修复
- [x] 前锋线蓝色 #1890ff + _progressCheckDate 独立变量
- [x] clearNetworkOffsets 不再清除 _progressCheckDate（防止前锋线复位）
- [x] 虚工作 toggle showDummyArrows
- [x] 节点分层压缩（不同层连续排布，无空洞）
- [x] NODE_R = 10
- [x] 底部镜像时间标尺
- [x] 画布拖拽（#network-body 空白区域）
- [x] svg.ondblclick 双击（节点→OpenTaskEditor / 箭线→ShowAddTaskModal / 空白→ShowAddTaskModal）
- [x] canvasH 基于层数计算

### 待验证（用户反馈未通过）
- 底部标尺同页显示 — 已修但用户未确认
- 双击弹窗 — 已解绑 _netDotNet 条件但用户未确认

### 仍缺失（需在新会话继续）
- 字体大小调整（NODE_R=10 后需验证文本是否正常）
- canvasH 修正（底部标尺+图例余量）
- 缩放补偿（scale() 坐标转换）
- 节点拖拽缩放情形下的箭线跟随
- 甘特图标尺三档对齐网络图

### 关键文件
- JS: I:\NetPlan\src\NetPlan.Server\wwwroot\js\netplan.js
- C#: I:\NetPlan\src\NetPlan.Server\Pages\Project\Network.razor
- 备份: I:\NetPlan\temp\netplan.js.backup.20260510
- DB: I:\NetPlan\src\NetPlan.Server\netplan.db

### 服务
- URL: http://localhost:5000
- 启动: Start-Process dotnet run -WorkingDirectory I:\NetPlan\src\NetPlan.Server

### 经验教训
- 修改 JS/C# 后必须 commit 到 git
- git checkout 前先 backup
- 不要修复编码问题时用 git checkout，应手动修复乱码
- 每个阶段完成立即 commit + 备份
# 2026-05-10 会话记录（续）

## 当前状态快照

### 已完成修复（本轮会话 — 网络图 V6.1 对标）
- [x] 底部标尺移至最下层节点下方 1 层（bottomRulerY = maxY + 60）
- [x] 节点半径 NODE_R=11，字体 12px
- [x] 双击节点→与甘特图一致的编辑对话框（含资源/前置/后置）
- [x] 双击空白→添加任务对话框
- [x] 拖拽节点→弹出偏移输入框→确认后直接调 SyncNodeDrag 改日期
- [x] 前锋线/今日线不穿透底部标尺
- [x] 甘特图 loadDraft/saveDraft/clearDraft JS 函数补全
- [x] _netDotNet 每次渲染重试 setNetworkDotNet
- [x] BuildData/BuildDataInternal 重构消除重复
- [x] **C1a 事件合并** — 同 ES/EF 任务共享 T{timeOffset} 事件节点（真双代号）
- [x] C1b 虚工作 SVG 渲染 — 从旧 S/E ID 改为 T{offset} 格式
- [x] C1c 自由时差波形线 — 之前已存在，非关键工作 ff>0 显示金色波形
- [x] P1 前锋线着色 — updateProgressColors 适配新事件 ID
- [x] 分层修复 — 取消同行约束（真双代号中工作可跨层）
- [x] 邻接边去重 — addEdge() 函数防止 inDeg 重复递减
- [x] JSON 格式修复 — "}}}," → "}},"

### 重大架构变化
- **calculateTimeParams 完全重写**：从 S{id}/E{id} 独立节点 → T{timeOffset} 合并事件节点
- events 去重：同 ES/EF 值共享一个事件
- 虚工作：从 relation 推 pred.taskId.ef → T{predEf} → T{succEs}
- 分层：取消同行约束（合并事件后共享节点导致平层），改为纯拓扑 BFS
- updateProgressColors：从 events['S'+tid] 改为 _netLayout.events[eventId]

### 仍缺失
- 纵向微调节点位置（A1）
- 从节点拖出创建工作（C1d）
- SyncNodeDrag 参数类型不匹配
- 缩放拖拽补偿
- 单元测试

### 关键文件
- JS: I:\NetPlan\src\NetPlan.Server\wwwroot\js\netplan.js
- C#: I:\NetPlan\src\NetPlan.Server\Pages\Project\Network.razor
- V6.1 清单: I:\NetPlan\memory\2026-05-10-v61-checklist.md
- Todo: I:\NetPlan\memory\2026-05-10-todo.md
- DB: I:\NetPlan\src\NetPlan.Server\netplan.db

### 服务
- URL: http://localhost:5000
- 启动: Start-Process dotnet run -WorkingDirectory I:\NetPlan\src\NetPlan.Server
- 编译: dotnet build --no-restore I:\NetPlan\src\NetPlan.Server

### 经验教训
- netplan.js 修改后用 `node --check netplan.js` 验证语法
- 写替换内容时注意 C#/JS 中的 Unicode 编码不一致（如中文标点）
- 事件合并重构是关键架构变动，必须同步更新所有依赖（buildNetworkSvg、calculateVerticalLayout、updateProgressColors、_netUpdateDummys）
- _netDayWidth 在 renderNetwork 中设置，_showDayPopup 中用 _netDayWidth 计算天数
- Blazor JSInvokable 方法不能有重复的 [JSInvokable] 属性

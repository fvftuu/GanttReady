# 2026-05-09 会话记录

## V6.1 功能对标分析
- 用户要求基于 V6.1 用户手册与 NetPlan 现状做功能对标，整理工作计划
- 已排除 U 系列（导航图、多日历、撤销恢复），用户明确不需要
- 已完成 F1/F2：节点拖拽角色区分 + 拖拽后箭线标签更新

## C1 实现：时标双代号图增加工作（4种方式）
- 时间：20:36-20:46
- JS 端：在 `netplan.js` 末尾新增 `_netC1Setup` 函数，监听 SVG 事件
  - SVG 空白双击 → 独立工作（从项目开始日期起）
  - 箭线右端双击（`data-end-area`） → 紧后工作，自动 FS 关系
  - 箭线左端双击（`data-start-area`） → 紧前工作
  - 空白区域拖拽画线 → 指定时间段工作，自动预设工期
  - HTML 面板：名称 + 工期输入 + 前置信息自动显示
- C# 端：`Network.razor` 新增 `AddTaskFromNetwork` JSInvokable
  - 创建 TaskItem → 创建 TaskRelation（如有前置）→ 调 CalculateScheduleAsync → BuildData
  - Editable=false, IsManualSchedule=false
- CSS 端：`site.css` 新增 `.net-task-panel` 样式（浮动面板）
- Build 成功 0 error，服务运行在 `http://localhost:5000`

## 已记录待办
- 文件：`I:\NetPlan\.workbuddy\tasks\2026-05-09-v6-features.md`
- C2-C6：横道图增加工作 / 逻辑双代号+单代号 / 流水布图 / 子网 / 多视图同步
- A1-A5：位置调整 / 空行 / 属性设置 / 显示内容 / 颜色线宽
- E1-E6：劳动力曲线 / 分组 / 过滤 / 输出文件 / 行标注 / 打印

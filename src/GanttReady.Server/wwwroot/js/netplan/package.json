{
  "scripts": {
    "clean": "rm -rf dist ../dist-netplan",
    "tsc": "tsc",
    "build": "tsc && node esbuild.config.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "happy-dom": "^20.9.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
